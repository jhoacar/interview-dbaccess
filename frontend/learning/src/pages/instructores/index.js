import InstructorContainer from "../../containers/instructors";

const InstructorPage = ({...props}) => {
  return (
  <InstructorContainer {...props}></InstructorContainer>);
};

export default InstructorPage;
